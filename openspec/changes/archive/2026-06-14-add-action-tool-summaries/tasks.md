## 1. Add summaries to action helper endpoints

- [x] 1.1 Add summary to `next_step` endpoint
- [x] 1.2 Add summary to `prev_step` endpoint
- [x] 1.3 Add summary to `draw_card` endpoint
- [x] 1.4 Add summary to `mulligan_draw_hand` endpoint
- [x] 1.5 Add summary to `move_card` endpoint
- [x] 1.6 Add summary to `set_card_property` endpoint (with warning)
- [x] 1.7 Add summary to `set_player_count` endpoint
- [x] 1.8 Add summary to `load_cards` endpoint
- [x] 1.9 Add summary to `unload_cards` endpoint
- [x] 1.10 Add summary to `exhaust_card` endpoint
- [x] 1.11 Add summary to `ready_card` endpoint
- [x] 1.12 Add summary to `flip_card` endpoint

## 2. Add summaries to phase-related endpoints

- [x] 2.1 Add summary to `deal_encounter` endpoint
- [x] 2.2 Add summary to `draw_boost` endpoint
- [x] 2.3 Add summary to `shuffle_into_deck` endpoint
- [x] 2.4 Add summary to `zero_tokens` endpoint
- [x] 2.5 Add summary to `modify_tokens` endpoint
- [x] 2.6 Add summary to `shadows_of_the_past` endpoint
- [x] 2.7 Add summary to `player_end_phase` endpoint
- [x] 2.8 Add summary to `villain_encounter_phase` endpoint
- [x] 2.9 Add summary to `villain_end_phase` endpoint
- [x] 2.10 Add summary to `multiple_double_sided_villains` endpoint
- [x] 2.11 Add summary to `discard_minion` endpoint
- [x] 2.12 Add summary to `discard_side_scheme` endpoint

## 3. Verify changes

- [x] 3.1 Run unit tests to verify endpoints still work
- [x] 3.2 Run lint to verify code formatting
- [x] 3.3 Verify OpenAPI schema includes summaries